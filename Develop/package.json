{
  "name": "literary-locator-mern",
  "version": "1.0.0",
  "description": "A MERN stack book search engine refactored to use GraphQL.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "install": "cd server && npm i && cd client && npm i",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "Lora",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}